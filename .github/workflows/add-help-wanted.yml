name: Add Help Wanted Label

on:
  issues:
    types: [labeled]

permissions: read-all  # default read; writes scoped to job below
jobs:
  label:
    permissions:
      issues: write
    uses: kubestellar/infra/.github/workflows/reusable-add-help-wanted.yml@1a04a3fd17771e78e5cf6d42a3fda84e3ba478f3 # pinned 2026-06-29 from main
    secrets: inherit
