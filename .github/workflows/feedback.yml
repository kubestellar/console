name: Feedback Wanted

on:
  pull_request:
    types: [closed]

permissions:
  contents: read
  pull-requests: write

jobs:
  feedback:
    uses: kubestellar/infra/.github/workflows/reusable-feedback.yml@88f4d5fff50ceea648cdd3848ed1835ebded5fe3 # main
    secrets: inherit
