name: Copilot DCO

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions: read-all  # default read; writes scoped to job below

jobs:
  copilot-dco:
    permissions:
      contents: read
      pull-requests: write
    uses: kubestellar/infra/.github/workflows/reusable-copilot-dco.yml@af322d48cc67ade7345cabc6a0ed55c6c7040ee4  # main
