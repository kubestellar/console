apiVersion: apps/v1
kind: Deployment
metadata:
  name: kubestellar-console
spec:
  replicas: 1
  progressDeadlineSeconds: 900
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/component: server
  template:
    metadata:
      labels:
        app.kubernetes.io/component: server
    spec:
      serviceAccountName: kubestellar-console
      automountServiceAccountToken: true
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      initContainers:
        - name: db-restore
          image: ghcr.io/kubestellar/console:latest
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
            runAsGroup: 1001
            runAsNonRoot: true
            runAsUser: 1001
            seccompProfile:
              type: RuntimeDefault
          resources:
            limits:
              cpu: 200m
              memory: 256Mi
            requests:
              cpu: 50m
              memory: 64Mi
          command:
            - /bin/sh
            - -c
            - |
              DB_PATH="/app/data/console.db"
              BACKUP_DIR="/backups"
              # Schema-only DBs are <=200KB; anything larger has real data
              MIN_DATA_SIZE=200000

              # get_file_size: portable file size using stat (GNU/BSD) or wc -c fallback
              get_file_size() {
                stat -c%s "$1" 2>/dev/null || stat -f%z "$1" 2>/dev/null || wc -c < "$1" 2>/dev/null | tr -d ' ' || echo "0"
              }

              is_valid_sqlite() {
                [ -f "$1" ] || return 1
                sqlite3 "$1" "PRAGMA quick_check;" 2>/dev/null | grep -qx "ok"
              }

              quarantine_corrupt_db() {
                if [ -f "$DB_PATH" ]; then
                  CORRUPT_PATH="${DB_PATH}.corrupt.$(date +%Y%m%d-%H%M%S)"
                  echo "Database appears corrupt, moving it to: $CORRUPT_PATH"
                  mv "$DB_PATH" "$CORRUPT_PATH"
                fi
              }

              if [ -f "$DB_PATH" ]; then
                DB_SIZE=$(get_file_size "$DB_PATH")
                if [ "$DB_SIZE" -gt "$MIN_DATA_SIZE" ] && is_valid_sqlite "$DB_PATH"; then
                  echo "Database exists with data (${DB_SIZE} bytes), skipping restore"
                  exit 0
                fi
                if [ "$DB_SIZE" -gt "$MIN_DATA_SIZE" ]; then
                  echo "Database exists but failed SQLite integrity check (${DB_SIZE} bytes)"
                  quarantine_corrupt_db
                else
                  echo "Database exists but appears empty (${DB_SIZE} bytes)"
                fi
              else
                echo "No database file found at $DB_PATH"
              fi

              # Find the latest valid backup
              LATEST=""
              for candidate in $(ls -1t "$BACKUP_DIR"/console-*.db 2>/dev/null); do
                CANDIDATE_SIZE=$(get_file_size "$candidate")
                if [ "$CANDIDATE_SIZE" -le "$MIN_DATA_SIZE" ]; then
                  echo "Skipping backup $candidate: empty/small (${CANDIDATE_SIZE} bytes)"
                  continue
                fi
                if ! is_valid_sqlite "$candidate"; then
                  echo "Skipping backup $candidate: failed SQLite integrity check"
                  continue
                fi
                LATEST="$candidate"
                BACKUP_SIZE="$CANDIDATE_SIZE"
                break
              done

              if [ -z "$LATEST" ]; then
                echo "No valid backups found in $BACKUP_DIR, starting fresh"
                rm -f "$DB_PATH"
                exit 0
              fi

              echo "Restoring from backup: $LATEST (${BACKUP_SIZE} bytes)"
              mkdir -p "$(dirname "$DB_PATH")"
              cp "$LATEST" "${DB_PATH}.tmp" && mv "${DB_PATH}.tmp" "$DB_PATH"
              if ! is_valid_sqlite "$DB_PATH"; then
                echo "ERROR: Restored database failed SQLite integrity check"
                exit 1
              fi
              echo "Restore complete"
          volumeMounts:
            - name: data
              mountPath: /app/data
            - name: backups
              mountPath: /backups
              readOnly: true
      containers:
        - name: kubestellar-console
          image: ghcr.io/kubestellar/console:latest
          imagePullPolicy: Always
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
            runAsGroup: 1001
            runAsNonRoot: true
            runAsUser: 1001
            seccompProfile:
              type: RuntimeDefault
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          # Startup probe gives the watchdog reverse proxy time to initialize
          # before liveness/readiness probes begin. Without this, the pod may
          # enter CrashLoopBackOff on slow nodes.
          startupProbe:
            httpGet:
              path: /watchdog/health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 12
          livenessProbe:
            httpGet:
              path: /watchdog/health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /watchdog/ready
              port: http
            initialDelaySeconds: 15
            periodSeconds: 5
            timeoutSeconds: 5
          resources:
            limits:
              cpu: 500m
              memory: 1Gi
            requests:
              cpu: 100m
              memory: 256Mi
          env:
            - name: PORT
              value: "8080"
            - name: BACKEND_PORT
              value: "8081"
            - name: DATABASE_PATH
              value: /app/data/console.db
            - name: KUBESTELLAR_OPS_PATH
              value: /usr/local/bin/kubestellar-ops
            - name: KUBESTELLAR_DEPLOY_PATH
              value: /usr/local/bin/kubestellar-deploy
            # Required in production mode. Create before applying:
            #   kubectl -n kubestellar-console create secret generic \
            #     kubestellar-console-jwt --from-literal=jwt-secret="$(openssl rand -base64 48)"
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: kubestellar-console-jwt
                  key: jwt-secret
            # Suppress local kc-agent connections — in-cluster deployments
            # accessed via port-forward or ingress have no local kc-agent
            # on the user's machine. (#10267)
            - name: NO_LOCAL_AGENT
              value: "true"
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: AI_DEFAULT_MODE
              value: medium
            - name: AI_TOKEN_LIMITS_ENABLED
              value: "true"
            - name: AI_MONTHLY_TOKEN_LIMIT
              value: "100000"
            - name: AI_TOKEN_WARNING_THRESHOLD
              value: "80"
            - name: AI_TOKEN_CRITICAL_THRESHOLD
              value: "95"
            - name: SELF_UPGRADE_ENABLED
              value: "true"
          volumeMounts:
            - name: kc-config
              mountPath: /app/.kc
            - name: tmp
              mountPath: /tmp
            - name: data
              mountPath: /app/data
      volumes:
        - name: kc-config
          emptyDir: {}
        - name: tmp
          emptyDir: {}
        - name: data
          persistentVolumeClaim:
            claimName: kubestellar-console
        - name: backups
          persistentVolumeClaim:
            claimName: kubestellar-console-backups
