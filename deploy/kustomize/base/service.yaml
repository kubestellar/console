apiVersion: v1
kind: Service
metadata:
  name: kubestellar-console
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/component: server
