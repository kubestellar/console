// Tests for MCP resources security allowlist and component classification
// have been moved to pkg/api/handlers/mcp/tool_allowlist_test.go where the
// tested symbols (validateToolName, AllowedOpsTools, AllowedDeployTools,
// classifyComponent) are actually defined (package mcp).
package handlers
